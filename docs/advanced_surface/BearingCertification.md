# G-011G Bearing Certification

Run `tool/advanced_surface_certification.dart` with the OpenCascade bridge, `bearing.stl`, and a project directory. It validates real selection, preview, Match, Rebuild, Heal, Gap Analysis, Network Analysis, validation, commit and rollback.

Each missing native operation must return its explicit `UnsupportedOperation` diagnostic while retaining the original handle and zero fallbacks/approximations.
