# Reconstruction Advisor

`ReconstructionUiState` prepares workflow progress, next step, dependencies, confidence, alternatives and explanation for Mobile or Desktop UI. It has no widget dependency and does not mutate the workflow.
