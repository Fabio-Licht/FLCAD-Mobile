# Architecture

The dependency flow is Geometric Kernel and domain engines → AREI → Engineering DNA → Engineering Cognition → Autonomous Reconstruction. The planner is pure, the dependency graph validates, the scheduler controls transitions, and the API coordinates cache and events. Engineering Core integration writes history, graph and platform events.
