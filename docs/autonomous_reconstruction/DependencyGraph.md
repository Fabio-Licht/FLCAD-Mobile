# Dependency Graph

The dependency graph rejects missing nodes and cycles and produces a deterministic topological order. A failed or cancelled dependency blocks every transitive dependent, preventing out-of-order reconstruction.
