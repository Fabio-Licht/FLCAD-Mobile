# Integration

`AutonomousReconstructionApi` is registered in `EngineeringContext.standard`. FEL exposes build, plan, inspect, explain, update, stage, pause, resume and rebuild vocabulary. `EXECUTE STAGE` is limited to non-CAD orchestration stages; surface, CAD feature and solid stages remain plans for future authorized engines.
