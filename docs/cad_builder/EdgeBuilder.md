# Edge Builder

`EdgeBuilder` creates lines from two vertices and arcs from start, middle and end vertices. Dependencies are passed as opaque handles and recorded in the topology graph. Curve construction remains a kernel responsibility.

