# Face Builder

`FaceBuilder.planar` creates a planar face from a wire, plane origin and plane normal. The kernel validates closure, orientation and degeneration before commit. Open or invalid boundaries produce diagnostics instead of a stored face.

