# Shell Builder

`ShellBuilder.sew` requests shell construction from face handles with an explicit sewing tolerance. The OpenCascade adapter translates this operation inside its private native bridge. The returned shell records whether it is closed; no automatic repair is applied.

