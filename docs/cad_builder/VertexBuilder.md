# Vertex Builder

`VertexBuilder.point(x, y, z)` requests a three-dimensional vertex from the active kernel. The resulting opaque handle receives a persistent project identity, validation, audit history and project storage before it is returned as successful.

