# Wire Builder

`WireBuilder` accepts one or more edge handles and an explicit open/closed intent. Kernel validation checks closure and orientation. An invalid wire is rolled back and is never persisted as a valid entity.

