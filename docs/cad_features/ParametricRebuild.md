# Parametric Rebuild

The independent Feature Graph identifies direct consumers of a changed shape and their downstream features. Rebuild evaluates only that affected set in dependency order, remaps rebuilt output handles and increments the existing Feature ID revision.

Parameters, user attribution and human decisions are copied into the new revision. Unrelated features are not recalculated.

