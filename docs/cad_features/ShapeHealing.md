# Shape Healing

Healing integrates with the existing interchange kernel contract. It records detected problems, proposed fixes, executed actions and the optional resulting handle in `HealingAudit`.

G-004D does not silently apply proposals. With no supporting backend, healing fails explicitly.

