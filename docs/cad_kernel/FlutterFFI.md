# Flutter FFI

The binding loads the platform-specific FLCAD OCCT library. Allocations are caller-owned and released deterministically. Only tokens, diagnostics and portable values cross the boundary.
