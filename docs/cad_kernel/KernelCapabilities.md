# OpenCascade Capabilities

The adapter declares STEP, IGES, BREP, healing and meshing. It does not declare boolean, fillet, chamfer, loft, sweep or GPU capabilities in G-004B.

Capability discovery describes the adapter surface; health status independently indicates whether the native host is installed and usable.

