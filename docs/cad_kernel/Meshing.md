# OpenCascade Meshing

Meshing delegates B-Rep tessellation to the native OCCT bridge with an explicit deflection and output path. The result reports vertex count, triangle count and payload location.

No mesh optimization, decimation or CAD modeling operation is included.

