# Native Bridge

`flcad_occ_api.h` is the only native boundary. It exports lifecycle, topology, analytic surfaces, interchange, validation, healing proposals, sewing and meshing. Exceptions become explicit errors.
