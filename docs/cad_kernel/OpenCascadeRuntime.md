# OpenCascade Runtime

OCCT is an optional backend loaded by `OpenCascadeFFI`. Lifecycle, version, health and capabilities cross a stable C ABI. Without the native library the kernel reports explicit unavailability and creates no synthetic shape.
