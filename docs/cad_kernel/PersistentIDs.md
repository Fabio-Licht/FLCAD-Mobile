# Persistent IDs

`PersistentIdService` creates and validates stable textual identifiers from project scope, entity kind and a generated component. IDs never use process memory addresses or vendor object pointers.

Persistent IDs are the basis for serialization, history, dependencies, collaboration and future Mobile/Desktop/Cloud synchronization.

