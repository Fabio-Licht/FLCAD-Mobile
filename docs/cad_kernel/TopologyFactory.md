# Topology Factory Contracts

Topology contracts describe wire, face, shell and solid builders. Builders accept persistent handles and transaction context, while the active plugin owns native topology construction.

Inputs are structural intent only. This layer performs no sewing, B-Rep construction, boolean operation or vendor conversion.

