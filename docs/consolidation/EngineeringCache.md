# Engineering Cache

The cache uses explicit domain namespaces (`geometry`, `reference`, `regions`, `surface`, `sketch`, `topology`, `reconstruction`, `ai`, `knowledge`, `cognition`, `project`). Entries support TTL, fingerprint and schema version validation. Statistics expose hits, misses, evictions and hit rate. Compression is intentionally a future storage adapter, not simulated behavior.
