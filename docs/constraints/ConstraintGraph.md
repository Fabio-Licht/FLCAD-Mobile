# Constraint Graph

Constraint, dependency, reference, and construction graphs are local to the solver domain. Dependency edges reject self-links and any edge that would make a directed cycle. Dirty propagation follows downstream edges.
