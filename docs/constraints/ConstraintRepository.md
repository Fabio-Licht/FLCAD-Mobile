# Constraint Repository

Explicit persistence writes constraints, graphs, history, analytics, and dimensions beneath `CAD/Constraints`, `CAD/ConstraintGraph`, `CAD/ConstraintHistory`, `CAD/ConstraintAnalytics`, and `CAD/Dimensions`. Persistent IDs are retained in JSON while filenames are sanitized for Windows.
