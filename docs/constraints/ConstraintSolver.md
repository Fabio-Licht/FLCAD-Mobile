# Constraint Solver

The incremental solver orders dirty constraints by priority, validates references and duplicates, applies supported parametric relations, and returns immutable statistics and diagnostics. Partial solve accepts explicit constraint IDs. It is async-ready and kernel-independent.
