# Degrees of Freedom

The editor reads entity and constraint state to report remaining freedom, X/Y translation, rotation, solver status, live analytics, and diagnostics. It never solves constraints itself.
