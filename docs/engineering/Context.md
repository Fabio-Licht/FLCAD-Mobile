# Engineering Context

EngineeringContext scopes Project, Runtime, Events, Commands, Queries, History, Cache, Graph, Kernel, Learning, Services, Configuration and User Session. Engines accept this context optionally for source compatibility. New engines must require it; existing engines can migrate constructor-by-constructor before legacy dependencies are removed.
