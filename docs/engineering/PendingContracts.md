# Pending Contract Scanner

The scanner found 17 explicit pending/unsupported markers. Categories:

- Geometry kernel: B-Rep execution, real booleans and solid validation.
- Surface adapters: tangent constraints and surface projection.
- Topology kernels: merge, split, repair and rebuild adapters.
- GPU: CUDA, Metal, Vulkan and OpenCL implementations.
- Cloud: distributed execution and synchronization providers.
- AI: production recognizers, predictors, classifiers and optimizers.
- Retention: persistent EPB replay and bounded event/history policies.

These are intentional contracts, not silently successful placeholders.
