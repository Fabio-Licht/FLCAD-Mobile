# Architecture

The dependency flow is Geometric Kernel and Smart Regions → AREI → Engineering DNA → Engineering Cognition. The orchestrator composes specialized recognizers and reasoners; API, runtime, cache, events, serialization, plugins and learning remain replaceable boundaries. `EngineeringCognitionIntegration` connects snapshots to Engineering Core history, graph and events without adding UI dependencies.
