# Decision Graph

Each decision stores identity, project, type, origin, evidence, confidence, priority, impact, dependencies, alternatives, estimated cost, expected benefit, risk, justification, time and responsible actor. Dependency edges are mirrored into `EngineeringGraph`, where cycles and missing prerequisites are rejected and downstream impact is queryable.
