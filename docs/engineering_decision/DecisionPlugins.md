# Decision Plugins

Plugins may provide evidence and criteria evaluators through a versioned, compatibility-checked registry. ONNX and TFLite are prepared as evaluator contracts only; no model runtime or inferred capability is bundled. Duplicate and incompatible plugins are rejected.
