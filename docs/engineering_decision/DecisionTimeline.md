# Decision Timeline

Creation and every human override append a sequenced timeline entry containing decision, revision, action, actor, reason and timestamp. The list is replay data and never silently rewrites past entries.
