# Engineering Ontology

The core ontology relates part, feature, surface, reference, function, process, inspection and manufacturing concepts. `EngineeringOntology` supports extension, ancestry and `isA` queries. Domain plugins may add concepts but cannot create relations whose endpoints are unknown.
