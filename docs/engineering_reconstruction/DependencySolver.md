# Dependency Solver

`ERIReconstructionGraph` validates prerequisites, rejects missing dependencies and cycles, topologically orders by priority, finds downstream impact and removes structurally redundant nodes. This DAG is the authoritative plan dependency model.
