# Command Palette

Commands have stable IDs, labels, categories, keywords, enablement and asynchronous execution. The palette searches all fields. Shortcut bindings are customizable by key, profile and context; Tool Manager filters tools by workspace context.
