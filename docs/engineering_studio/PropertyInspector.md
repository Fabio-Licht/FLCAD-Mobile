# Property Inspector

The inspector projects selected tree-node context into Identity, Engineering and Analytics sections. It exposes origin, normal, area, confidence, DNA, dependencies, visibility and lock state when supplied by the owning domain.
