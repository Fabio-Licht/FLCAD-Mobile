# Selection Manager

Selection supports single, multiple, box and lasso mode contracts, type filters, invert, expand, shrink, similar entities, confidence threshold and entity type. Lasso geometry is delegated to `LassoSelectionBackend` and is not approximated by the UI.
