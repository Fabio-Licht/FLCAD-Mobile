# Workspace Manager

The manager tracks multiple open projects and one active project, each with an independent Layout Manager. Desktop Runtime schedules background work through Engineering Runtime 2.0. Native multi-window behavior requires an installed `NativeWindowBackend` and otherwise fails explicitly.
