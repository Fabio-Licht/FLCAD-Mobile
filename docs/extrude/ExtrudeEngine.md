# Extrude Engine

The engine prepares, previews, confirms, rebuilds, suppresses and rolls back Extrude features. Confirmation routes through the official GeometryKernelAPI transaction and records the resulting native `ShapeHandle` only after validation and commit.
