# Extrude Timeline

Every Extrude registers a G-007A Feature instance with persistent identity, parameters, timestamp, dependencies and execution state. Dirty changes participate in partial and incremental rebuild queues.
