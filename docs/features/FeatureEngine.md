# Feature Engine

The engine manages persistent feature contracts, inputs, references, parameters, metadata, diagnostics, execution state, transactions, undo/redo, and explicit unavailable results. It creates no geometry.
