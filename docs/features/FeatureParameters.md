# Feature Parameters

Named parameters include units, values, defaults, formulas, references, and normal, locked, driven, or driving modes. Direct named references resolve now; general equations are retained with explicit diagnostics for a future evaluator.
