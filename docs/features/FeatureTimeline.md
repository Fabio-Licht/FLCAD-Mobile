# Feature Timeline

Timeline order tracks execution position, rollback points, rebuild queue, and pending, queued, rebuilding, ready, suppressed, frozen, failed, edited, unsupported, and kernel-unavailable states.
