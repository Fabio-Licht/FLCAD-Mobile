# Computational Geometry

`GeometryAlgorithms` provides distance, closest point, plane projection, line/plane intersection, centroid, barycentric coordinates and point-in-polygon support. `RobustPredicates` centralizes orientation and adaptive epsilon behavior. Convex hull and collision algorithms are extension boundaries until production implementations are supplied.
