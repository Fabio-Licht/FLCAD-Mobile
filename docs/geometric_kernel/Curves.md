# Curves

All curves implement `ParametricCurve3` with domain, evaluation and first derivative. Implemented forms are line, circle, arc, ellipse, Bézier, B-spline, helix and polyline. B-spline evaluation uses de Boor's algorithm. NURBS remains a mathematical extension point pending the rational control-point representation required by the surface program.
