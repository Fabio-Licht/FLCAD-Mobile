# Numerical Methods

The numerical layer includes Newton iteration, gradient descent, convergence criteria and RMS/maximum error metrics. Gauss-Newton, Levenberg-Marquardt, RANSAC and ICP expose contracts because their parameterization and domain-specific residual models must be supplied by consumers; the foundation does not fabricate results.
