# Precision

`Tolerance` carries absolute, relative and angular tolerances. `PrecisionContext` adds units, display precision and scale-adaptive epsilon. Validation detects non-finite values and degeneracies. Algorithms use normalized directions and tolerance-aware predicates at decision boundaries.
