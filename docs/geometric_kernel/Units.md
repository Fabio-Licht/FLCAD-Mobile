# Units

The unit system supports millimeters, centimeters, meters, inches, degrees and radians. `UnitConverter` performs explicit conversions; geometry stores unit-neutral numeric values and the owning project supplies its `PrecisionContext`. This prevents hidden conversions across Mobile, Desktop and Cloud.
