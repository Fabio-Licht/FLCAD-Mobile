# Recognition Competition

Competition ranks validated candidates by score, removes duplicates for the same primitive/region, retains alternatives and reports rejected duplicates. Failed or unsupported recognizers cannot win. When none validate, URF emits `unknown` with zero confidence.
