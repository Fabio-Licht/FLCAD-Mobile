# Recognition DNA

Every result records primitive type, parameters, region, deterministic geometric signature, quality, fused confidence, evidence, recognizer origin and framework version. Cache keys include mesh fingerprint, region fingerprint and ordered parameters.
