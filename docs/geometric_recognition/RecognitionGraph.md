# Recognition Graph

Results and source regions are added to a dedicated graph backed by Engineering Graph. The `recognized-as` edge supports impact analysis and future links to references, sketches, surfaces and features without coupling recognizers to those domains.
