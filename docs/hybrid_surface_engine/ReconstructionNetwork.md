# Reconstruction Network

The reconstruction DAG assigns each candidate a future builder contract, dependencies, continuity target, validation contract, healing-proposal contract and deferred shape-generation stage.

Every `shapeGeneration` value remains `deferred-to-kernel` in G-005C. Execution belongs to later OpenCascade and professional reconstruction integration.

