# Surface Network

Each network node retains its `SurfaceCandidate`, neighbors, shared boundaries, predicted continuity, dependencies, priority and geometric influence. Edges model shared boundary and regional adjacency without representing CAD topology.

