# Adaptive Compensation

`AdaptiveCompensationEngine` converte quantidade, direção, mapas de pressão/espessura e processo em um `MorphRequest` ponderado. Constraints limitam deslocamento e congelam features. Não executa simulação física.
