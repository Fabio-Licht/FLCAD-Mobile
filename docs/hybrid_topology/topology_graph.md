# Topology Graph

TopologyGraph reutiliza o REKG tipado, evitando um segundo grafo incompatível. Mesh → Region → Reference → Sketch → Surface → Solid → CAM pode ser navegado por dependência e impacto, mantendo Solid/CAM apenas como nós futuros.
