# Adaptive Topology Solver

O solver classifica Relax, Smooth e Curvature Flow conforme ruído, curvatura e preservação. `MeshMorphEngine` produz deltas e respeita frozen/max displacement. Novos solvers entram por runtime CPU, GPU ou Cloud.
