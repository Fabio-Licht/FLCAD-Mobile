# Dependency Graph

The graph contains recognition regions, patches, boundaries, continuity relations and patch-scoped reflection, zebra, draft, heat-map, validation and analytics nodes. Edges encode only declared dependencies. Downstream traversal begins from the operation preview, not from every project node.
