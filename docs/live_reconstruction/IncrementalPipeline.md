# Incremental Pipeline

An operation preview defines the dirty roots. The affected calculator expands only their dependency closure; validation checks that scope; the scheduler emits only those IDs. Unrelated connected components remain untouched. Commit is exclusively delegated through Surface Operations to `GeometryKernelAPI`.
