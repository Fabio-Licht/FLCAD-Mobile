# Update Scheduler

The scheduler orders affected patches, boundaries, continuity, reflection, zebra, draft, heat maps, validation and analytics. It rejects scope escape. It owns no background timer, isolate or worker and runs only from an explicit update command.
