# Mesh Entity

The first-class mesh entity owns persistent identity, source, checksum, counts, bounds, units, normals, orientation, import timing, native handle, health, validation and lifetime state.
