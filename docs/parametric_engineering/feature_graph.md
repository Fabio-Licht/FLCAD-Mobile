# Feature Graph

FeatureGraph reutiliza o REKG tipado para conectar Mesh → Region → Reference → Sketch → Surface → Feature → Solid → CAM → Inspection. Relações dirigidas permitem invalidação e reconstrução incremental.
