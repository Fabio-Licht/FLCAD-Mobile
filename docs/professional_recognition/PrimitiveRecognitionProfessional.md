# Professional Primitive Recognition

UERS extends URF with cylinder, cone and torus recognizers. Initial axes come from PCA; parameters are estimated from radial/axial samples; robust median-deviation filtering rejects gross outliers. Every candidate retains RMS, maximum/mean error, coverage, stability, score, parameters and evidence. Plane and sphere reuse URF implementations.
