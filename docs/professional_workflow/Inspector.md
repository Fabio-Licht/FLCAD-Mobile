# Engineering Inspector

`InspectorSnapshot` exposes mesh quality, open regions, normal consistency, coverage, confidence and reconstruction readiness. It is a read model; measurement engines remain the source of truth. Missing measurements stay zero/unknown rather than being simulated.
