# Workflow Advisor

Recommendations contain a title, action and an `ExplainableDecision`: why, weighted evidence with provenance, alternatives, confidence and impact. The advisor derives the next proposal from workflow state and is independent of widgets. Future AREI/Cognition adapters can provide evidence without changing UI contracts.
