# Workflow Analytics

Sessions persist under `Project/Sessions` with atomic serialized writes. Analytics report elapsed time, accepted recommendations, automations, estimated operations avoided and estimated time saved. Estimates use explicit deterministic policy (currently two minutes and three operations per recorded automation) and must not be presented as measured industrial benchmarks.
