# Confidence Engine

Confidence is constrained to zero through one. Evidence reliability provides weights and coverage calibration prevents sparse observations from appearing certain. Classification formulas are deterministic Alpha baselines designed to be replaceable by calibrated models.
