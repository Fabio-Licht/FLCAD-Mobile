# Learning Engine

User corrections are stored as `EngineeringExperience` records and forwarded to `EngineeringLearning`. The default memory is process-local; Project, Desktop or Cloud persistence can implement `EngineeringMemory` without changing the reasoning engine. Learning records corrections and never silently changes historical decisions.
