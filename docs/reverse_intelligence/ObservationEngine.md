# Observation Engine

The Alpha observer measures triangle area, bounding volume, mesh density, boundary edges, normal coherence, principal extents and centroid. Observation never reconstructs or repairs the mesh. Degenerate and empty inputs fail explicitly.
