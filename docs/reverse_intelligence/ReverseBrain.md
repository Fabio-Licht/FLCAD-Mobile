# Reverse Brain

`ReverseBrain` performs a deterministic reasoning cycle over a mesh. Every probability references evidence; every selected strategy reports confidence, cost and utility. `ReconstructionOrchestrator` adds lifecycle events and relationships in the reverse knowledge graph.
