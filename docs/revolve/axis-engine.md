# Axis engine

Axes carry origin, normalized direction intent, reference kind, optional persistent reference and reversal. Zero or non-finite directions are rejected. Axis reversal changes parametric intent without creating geometry.
