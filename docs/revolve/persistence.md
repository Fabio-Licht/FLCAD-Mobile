# Persistence

Projects store feature definitions, parameter snapshots, history, analytics and preview metadata under the five `CAD/Revolve*` directories. Geometry handles remain references to kernel-owned data.
