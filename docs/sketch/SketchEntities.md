# Sketch Entities

The foundation supports point, line, circle, arc, spline control points, ellipse, construction geometry, and reference geometry. Every entity owns a persistent UUID-based ID, metadata, timestamp, owner, version, history references, graph node, selection/visibility/lock flags, classification flags, diagnostics, and parametric values.
