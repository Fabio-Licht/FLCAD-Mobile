# Sketch Graph

Sketch, entity, dependency, reference, and construction graphs are distinct instances. They reject unknown nodes and cycles. They intentionally have no dependency on the global Engineering Graph.
