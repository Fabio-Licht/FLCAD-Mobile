# Curvature Engine

OpenCascade samples the parameter domain of each `TopoDS_Face` with `BRepLProp_SLProps`. The report includes minimum and maximum principal curvature, their averages, mean and Gaussian curvature, equivalent radius, normal-derived curvature gradient and stability. Invalid or empty differential sampling fails explicitly.
