# Cone Fitter

Cone fitting performs robust weighted regression between axial height and radial distance. Iterative slope refinement yields the semi-angle, apex, axis, bounds and pointwise residuals used by `gp_Cone`.
