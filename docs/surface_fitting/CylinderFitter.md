# Cylinder Fitter

Cylinder fitting projects points against the recognized axis, then iteratively minimizes radial residuals with robust weights. Axial extrema define native surface bounds; radius and axis remain explainable parameters.
