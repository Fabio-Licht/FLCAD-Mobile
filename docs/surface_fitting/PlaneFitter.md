# Plane Fitter

Plane fitting uses PCA least squares, deterministic residual outlier rejection and a second robust fit. Signed orthogonal distances produce RMS, maximum, mean and standard deviation before OCCT creates the bounded plane face.
