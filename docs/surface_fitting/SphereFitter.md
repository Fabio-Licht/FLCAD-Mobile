# Sphere Fitter

Sphere fitting uses least squares, deterministic inlier refinement and weighted radial minimization. Center, radius and full angular bounds are passed to the native sphere builder only after validation.
