# Surface Registry

Each generated surface records a Surface ID, persistent kernel handle, kind, origin, regions, evidence IDs, optional Feature ID, revision, timestamp, parameters, predicted continuity, confidence, validity and diagnostics.

Unavailable and invalid attempts are persisted in history and diagnostics but never enter the registry.

