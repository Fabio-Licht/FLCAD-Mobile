# Surface Graph

`SurfaceDependencyGraph` records dependency, continuity, reference, neighborhood and influence relationships between planned candidates. Dependency cycles are rejected; downstream impact remains queryable without CAD topology.

