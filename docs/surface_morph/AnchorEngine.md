# Anchor Engine

The engine validates unique fixed, soft, boundary, surface, curve, point and multi anchors. Each anchor has a real target ID, 3D position and normalized strength. Invalid positions, strengths and duplicate IDs fail explicitly.
