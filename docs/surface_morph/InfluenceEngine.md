# Influence Engine

Influence regions use a positive radius and Linear, Smooth, Gaussian, Bell or Custom Curve falloff. Weights are deterministic and remain preview metadata until an approved kernel commit.
