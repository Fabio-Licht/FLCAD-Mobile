# Morph Validation

Commit requires healthy topology, acceptable continuity and quality, existing affected patches, healthy boundaries, at least one anchor and no conflicting constraint group. Failure prevents creation of a Surface Operation.
