# Operation History

Creation, preview, validation, commit, unsupported result, rollback and cancellation are append-only history events. Operation records persist under `CAD/SurfaceOperations` and `CAD/OperationHistory`, with constraint, validation, analytics and report projections in their dedicated Project First paths.
