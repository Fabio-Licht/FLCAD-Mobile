# Surface Operations

The official operation types are Move Boundary, Extend, Trim, Split, Merge, Offset, Replace, Match, Project Boundary, Reparameterize and Healing. Each operation owns a persistent ID, target patch/surface, constraints, parameters, preview, status, timing, undo/redo tokens, validation and analytics. Unsupported kernel work is an explicit terminal status, never simulated geometry.
