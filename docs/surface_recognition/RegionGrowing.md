# Region Growing

Triangles are adjacent only when they share an indexed edge. Growth requires angular-normal and local-curvature continuity. Limits are configurable, disconnected components remain separate, and undersized regions merge only into a topological neighbor. Region IDs and colors are deterministic for a mesh checksum.
