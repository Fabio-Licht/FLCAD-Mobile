# Loop Detection

Every native wire becomes a loop. The first closed wire is outer; subsequent closed wires are inner unless classified as micro loops. Empty and open structures retain explicit health instead of being silently repaired.
