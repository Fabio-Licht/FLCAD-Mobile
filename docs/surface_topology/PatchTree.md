# Patch Tree

The patch tree exposes surface links, boundaries, loops, neighbors, intersections, confidence, health and status. Selection remains synchronized with highlight and the topology property projection.
