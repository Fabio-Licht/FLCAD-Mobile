# Surface Topology

The official pipeline is Surface Entity → native Boundary Extraction → native Intersection Analysis → Loop Detection → Adjacency Graph → Patch Construction → Topology Graph → Project First. No solid or final BRep is produced.
