# Transition Timeline

The shared history records creation, preview, confirmation, parameter edits, dependencies, rebuild, rollback, suppression and undo/redo with persistent IDs and timestamps. Ordering and impact use the Feature Modeling graphs.
